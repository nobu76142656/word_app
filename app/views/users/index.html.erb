
<div class="jumbotron">

  <table class="table">
    <tr bgcolor="silver">
      <th>名前</th><th>メールアドレス</th>
    </tr>
    <% @users.each do |user| %>
    <tr>
      <td><%= link_to user.name, user_path(user.id) %></td><td><%= user.email %></td>
    </tr>
    <% end %>
  </table>
</div>
